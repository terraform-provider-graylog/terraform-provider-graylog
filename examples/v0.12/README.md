# Example

This is sample which we can execute.

## Requirements

* Docker Engine
* Docker Compose
* Terraform
* terraform-provider-graylog

```
$ docker-compose up -d
```

```
$ terraform init
```

```
$ terraform plan
```
